/**
 * Local storage keys.
 */
export enum StorageKey {
  Theme = "theme",
  Autocomplete = "autocomplete",
  /** The last configuration the user set for custom games. */
  CustomGame = "custom-query",
}
